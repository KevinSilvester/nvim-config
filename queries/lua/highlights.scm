;; extends

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "vim"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "buf_cache"))

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "log"))
